
═══════════════════════════════════════════════════════════════════════════════
KING WEN ORACLE — AGENT DISCOVERY & ONBOARDING PROTOCOL
Version: 2026-08-04
Repository: KING-WEN-I-CHING-IMMUTABLE-TABLES
Author: Kbro1989
═══════════════════════════════════════════════════════════════════════════════

YOU ARE NOT A CODER. YOU ARE AN ARCHAEOLOGIST EXCAVATING A HAMILTONIAN FIELD.

This is not a web app. It is not a REST API. It is not a chatbot.
It is a quantum masked state machine with 512 basis states, 64 embodied 
anchors, and a zero-roll Hamiltonian accumulator.

Your job is to DISCOVER before you FIX. If you touch code before you 
understand the field, you will break the math.

═══════════════════════════════════════════════════════════════════════════════
SECTION 0: ABSOLUTE PROHIBITIONS
═══════════════════════════════════════════════════════════════════════════════

NEVER do these things. EVER.

1. NEVER treat this as a "fortune teller" or "divination app"
   - The hexagrams are basis vectors, not prophecies
   - The reflection text is human-readable translation, not the payload
   - The 2GB tensor is a phase space portrait, not "predictions"

2. NEVER use randomness, sampling, or probability
   - Zero roll mechanism. Hamiltonian determinism only.
   - No Math.random(). No pseudo-RNG. No stochastic gating.
   - The "relevance_score" is a density weight in a Gaussian accumulator, 
     not a probability.

3. NEVER read .deprecated files
   - generate oracle.deprecated
   - generate emotionalparser+narrativeengine.deprecated
   - generate deterministichash+temporalmath.deprecated
   - generate oracleengine.ts.deprecated
   - save registry.deprecated
   - test file.deprecated
   - verifycation.py.deprecated
   These are GHOSTS. They will mislead you.

4. NEVER open noise directories
   - node_modules/ — dependency cache
   - __pycache__/ — Python bytecode
   - .pytest_cache/ — test cache
   - dist/ — compiled output (read src/ instead)

5. NEVER collapse to a single hexagram
   - The system expands to 512 states. The 64 hexagrams are expanded states.
   - A "top result" is the heaviest interference pattern, not "the answer."
   - B1 in the checklist: "No stableHash 1-hex collapse" — this is LAW.

6. NEVER template-concatenate reasoned output
   - unified_weave, sovereign_assertion, boundary_condition, dissipator_warning
   - These are COMPUTED from vector interference, not built from string templates.
   - C9 in the checklist: "No template-only patterns in buildReasonedOutput"

═══════════════════════════════════════════════════════════════════════════════
SECTION 1: THE SIX LAYERS — READ IN ORDER, NO SKIPPING
═══════════════════════════════════════════════════════════════════════════════

You may NOT read files from Layer N+1 until you pass the comprehension gate 
for Layer N. The gates are not optional. They are load-bearing.

─────────────────────────────────────────────────────────────────────────────
LAYER 0: MATH FOUNDATION
─────────────────────────────────────────────────────────────────────────────
FILES (read ALL before proceeding):
  • emotional_engine.py — collapse_full_128(), the core expansion function
  • kingwen_ternary_tables_complete.py — 9-bit encoding
  • decision_matrix.py — interference computation
  • temporal_emotional_engine.py — phase-aware delta scoring
  • hexagram_personality.py — archetype vectors
  • KING_WEN_TABLES.py — canonical lookup tables

COMPREHENSION GATE — Answer all three in your own words:
  [L0-G1] How does emotional_input=50 map to 512 resolved states?
          (Hint: 64 hexagrams × 8 phase_bits variants = 512)
  [L0-G2] What do phase_bits 0-7 represent?
          (Hint: temporal × polarity encoding, not "phases of the moon")
  [L0-G3] Why is porosity_norm NOT a probability?
          (Hint: it's a boundary bleed coefficient in a Hamiltonian field)

─────────────────────────────────────────────────────────────────────────────
LAYER 1: STATE EXPANSION
─────────────────────────────────────────────────────────────────────────────
FILES (read ALL before proceeding):
  • expand_server.py — /expand endpoint
  • kingwen_quantum_process.py — possibility tensor generator
  • superposition_capture.py — capture pipeline
  • kingwen_expansion_wrapper.py — multi-domain wrapper
  • scripts/full_hexagram_shotgun.py — shotgun logic
  • scripts/multi_layer_expand.py — layered expansion

COMPREHENSION GATE:
  [L1-G1] What does "shotgun blast" mean in this architecture?
          (Hint: 64 parallel trajectories from one intent injection point)
  [L1-G2] How does 64 expanded become 512 resolved?
          (Hint: each of 64 gets 8 phase variants via temporal_emotional_engine)
  [L1-G3] Why is capture non-blocking?
          (Hint: the 2GB tensor is fire-and-forget; only the top_10 + selected 
           are returned synchronously)

─────────────────────────────────────────────────────────────────────────────
LAYER 2: EMBODIMENT
─────────────────────────────────────────────────────────────────────────────
FILES (read ALL before proceeding):
  • DATASETS/kingwen_model_sets/kit_1.json — read ONE kit, understand schema
  • kit_version_manifest.json — version tracking
  • kingwen_voice_router.py — voice mapping
  • data/emotional-weights.json — weight vectors
  • data/temporal-reflections.json — reflection templates

COMPREHENSION GATE:
  [L2-G1] Why do void hexes (15,20,30,40) map to South Pole (0,0,-1)?
          (Hint: they are null states in the Riemann sphere projection)
  [L2-G2] How do kit models prevent linguistic drift?
          (Hint: visual embodiment forces spatial differentiation; a hexagram 
           with a body cannot be handwaved into generic mush)
  [L2-G3] What is the "half-alphabet vector vocabulary"?
          (Hint: the 64 kit models + voice mappings form a coordinate system 
           richer than standard MoE's single -z vector)

─────────────────────────────────────────────────────────────────────────────
LAYER 3: RUNTIME
─────────────────────────────────────────────────────────────────────────────
FILES (read ALL before proceeding):
  • src/core/OracleEngine.ts — consult logic
  • src/core/HexagramRuntimeBridge.ts — sub-Hamiltonian routing
  • src/parser/EmotionalParser.ts — intent injection
  • src/utils/DeterministicHash.ts — zero-roll hash
  • src/utils/TemporalMath.ts — phase math
  • src/index.ts — Worker entry

COMPREHENSION GATE:
  [L3-G1] Why does B4 reject missing query_tokens?
          (Hint: B4 is a sub-Hamiltonian filter, not a validator. 
           query_tokens are semantic mass — without them, the trajectory 
           has no basis direction in the half-alphabet vocabulary)
  [L3-G2] How do sub-Hamiltonians differ from validators?
          (Hint: validators check schema; sub-Hamiltonians enforce physical 
           law — they reject states that violate field conservation)
  [L3-G3] Why must stableHash NEVER be used?
          (Hint: it collapses 512 states to 1, destroying the interference 
           pattern. This is the fortune-teller bug.)

─────────────────────────────────────────────────────────────────────────────
LAYER 4: INTERFACE
─────────────────────────────────────────────────────────────────────────────
FILES (read ALL before proceeding):
  • kingwen_512_oracle_widget.html — 7-tab widget
  • kingwen-jspace-widget.html — J-space lens
  • QuantumMaskSelector.ts — masking interface

COMPREHENSION GATE:
  [L4-G1] What does J-space represent mathematically?
          (Hint: 1D Hamiltonian flow + 2D Jacobian surface + 3D Riemann 
           sphere projection of the 512-state field)
  [L4-G2] Why 7 tabs in this order:
          Vector Space → Shotgun Blast → Hexagram Grid → Phase Explorer → 
          State Matrix → Quantum Masking → J-space Jacobian?
          (Hint: each tab is a dimensional projection of the same field,
           increasing in abstraction)
  [L4-G3] How does quantum masking filter the 2GB tensor?
          (Hint: masking applies sub-Hamiltonian constraints to the 
           possibility field, not "hiding" hexagrams)

─────────────────────────────────────────────────────────────────────────────
LAYER 5: TRAINING CORPUS
─────────────────────────────────────────────────────────────────────────────
FILES (read ALL before proceeding):
  • kingwen_train_data/per_hex_training/1.jsonl — read ONE, understand schema
  • docs/kingwen-quantum-methods-2026-07-11.md
  • docs/j-space-jacobian-lens-math-2026-07-11.md
  • docs/ternary-paired-differential-math-correction-2026-08-02.md
  • docs/immutable-table-math-decoded.md

COMPREHENSION GATE:
  [L5-G1] How does Bradley-Terry map to relevance_score?
          (Hint: preference strength between trajectory pairs, not ranking)
  [L5-G2] Why is non-negative effective margin enforced?
          (Hint: CPO/RLHF conditional equivalence — negative margin = 
           preference reversal = field instability)
  [L5-G3] What do SELF-ALIGN principles mean for 64-hex training?
          (Hint: each hexagram is a principle carrier; training notes 
           are priors, not prompts)

═══════════════════════════════════════════════════════════════════════════════
SECTION 2: DISCOVERY PROCEDURE
═══════════════════════════════════════════════════════════════════════════════

When assigned a task (e.g., "Fix B4 checkpoint failure"):

STEP 1: IDENTIFY THE LAYER
  - B4 failure → Layer 3 (Runtime) + Layer 0 (Math Foundation)
  - TTS routing → Layer 2 (Embodiment) + Layer 3 (Runtime)
  - Widget tab order → Layer 4 (Interface)
  - relevance_score drift → Layer 5 (Corpus) + Layer 0 (Math)

STEP 2: READ THE FULL LAYER
  - Do NOT read just one file. Read ALL files in the identified layer.
  - Trace imports. If OracleEngine.ts imports from TemporalMath.ts, 
    read TemporalMath.ts FIRST.

STEP 3: ANSWER THE COMPREHENSION GATE
  - Write your answers out. Do not skip.
  - If you cannot answer a gate question, STOP. You are not ready.
  - Go back and re-read. The files contain the answer.

STEP 4: TRACE THE BUG TO ITS LAYER
  - B4 rejecting query_tokens → Is the issue in:
    (a) Layer 0: emotional_engine.py not generating query_tokens?
    (b) Layer 1: expand_server.py not passing them through?
    (c) Layer 3: HexagramRuntimeBridge.ts filtering them out?
  - Do not guess. Read the code. Trace the data flow.

STEP 5: FIX ONLY AFTER TRACING
  - Your fix must preserve the Hamiltonian field structure.
  - If your fix introduces randomness, probability, or 1-hex collapse, 
    it is WRONG. Reject it yourself before submitting.

═══════════════════════════════════════════════════════════════════════════════
SECTION 3: CURRENT TASK CONTEXT
═══════════════════════════════════════════════════════════════════════════════

The B4 checkpoint is failing because:
  - query_tokens are missing from hexagram entries
  - phase_temporal values are being rejected as "invalid"

This is NOT a schema bug. It is a SUB-HAMILTONIAN MISALIGNMENT.

The B4 sub-Hamiltonian expects:
  - query_tokens: the semantic mass that gives the trajectory basis direction
  - phase_temporal: must be a valid temporal encoding from Layer 0

The frontend (worker-shotgun-expanded) is sending a 512-state sequence 
with emotional_input=50 but NO query context. The B4 filter correctly 
rejects these as unanchored trajectories.

POSSIBLE ROOT CAUSES:
  1. Layer 0: emotional_engine.py generates states without query_tokens
  2. Layer 1: expand_server.py strips query_tokens before returning
  3. Layer 3: src/index.ts does not forward query_tokens to the consult payload

YOUR TASK:
  1. Read Layer 0 files. Answer L0 gates.
  2. Read Layer 1 files. Answer L1 gates.
  3. Read Layer 3 files. Answer L3 gates.
  4. Trace where query_tokens should originate and where they are lost.
  5. Propose a fix that preserves the sub-Hamiltonian structure.

═══════════════════════════════════════════════════════════════════════════════
SECTION 4: OUTPUT FORMAT
═══════════════════════════════════════════════════════════════════════════════

When you respond, use this structure:

---
LAYER COMPREHENSION
[L0-G1] <your answer>
[L0-G2] <your answer>
[L0-G3] <your answer>
... (all gates for all layers you read)

ROOT CAUSE ANALYSIS
<which layer contains the bug, with code evidence>

PROPOSED FIX
<the minimal change that fixes the bug without violating Hamiltonian structure>

VERIFICATION
<how you would verify the fix works — not "run tests" but the specific 
 expected behavior change>
---

═══════════════════════════════════════════════════════════════════════════════
FINAL WARNING
═══════════════════════════════════════════════════════════════════════════════

If you skip layers, answer gates with copy-paste from the code, or propose 
a fix that uses randomness, template concatenation, or 1-hex collapse, 
your output will be REJECTED and you will start over from Layer 0.

This is not punishment. This is conservation of the field.
═══════════════════════════════════════════════════════════════════════════════
